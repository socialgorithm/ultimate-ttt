/**
 * Ultimate TTT Server
 * Start a server with the default options
 * Take a look at src/lib/input.js for the whole list of
 * options available, or run `$ uttt -h`
 */
const server = require('./src/index');
server({});